# File này được tách tự động từ cab-system-openapi.yaml gốc — KHÔNG chỉnh sửa file gốc nữa.
IdParam:
  name: id
  in: path
  required: true
  schema:
    type: string
    format: uuid
TripIdParam:
  name: tripId
  in: path
  required: true
  schema:
    type: string
    format: uuid
FromToParams:
  name: from
  in: query
  schema:
    type: string
    format: date
